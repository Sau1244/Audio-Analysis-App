files: '.py'
exclude: '.git'
default_stages: [pre-commit, manual]

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.13
    hooks:
      - id: ruff-check
        args: ["--select", "I", "--fix"]
      - id: ruff-format
